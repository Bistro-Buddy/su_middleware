# Render Proxy Server

This is a simple proxy server to bypass mixed content restrictions when calling insecure HTTP APIs from an HTTPS frontend.

## Endpoint

`POST /proxy`

### Body (JSON):

```json
{
  "name": "Alvis Abreo",
  "email": "alvis@example.com",
  "mobile": "9057574458",
  "sourceName": "EDXSPRT107",
  "campignName": "partnerotp"
}
```
